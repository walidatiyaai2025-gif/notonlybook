(()=>{const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.primary-nav');if(menu&&nav)menu.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',String(open))});const slides=[...document.querySelectorAll('.hero-slide')],dots=[...document.querySelectorAll('.hero-dot')];if(!slides.length)return;let active=0,timer;const show=i=>{active=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('is-active',n===active));dots.forEach((d,n)=>d.classList.toggle('is-active',n===active))};const play=()=>{timer=setInterval(()=>show(active+1),6500)};dots.forEach((d,i)=>d.addEventListener('click',()=>{clearInterval(timer);show(i);play()}));if(!matchMedia('(prefers-reduced-motion: reduce)').matches)play()})();
